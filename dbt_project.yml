analysis-paths:
- analyses
clean-targets:
- target
- dbt_packages
config-version: 2
macro-paths:
- macros
model-paths:
- models
models:
  pilot_testdb_databricks:
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
    staging:
      +materialized: view
      +schema: staging
name: pilot_testdb_databricks
profile: datamigrate_target
seed-paths:
- seeds
snapshot-paths:
- snapshots
test-paths:
- tests
version: 1.0.0
